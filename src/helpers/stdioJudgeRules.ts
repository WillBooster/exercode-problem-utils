/** Longest stdout (and stderr) the stdio judge accepts from one run; a longer output is rejected. */
export const MAX_STDOUT_LENGTH = 50_000;

/** The judge server shows a test case on the problem page when its id contains `example`. */
export const EXAMPLE_TEST_CASE_ID_PATTERN = /example/;
